{
  "name": "lasttee",
  "version": "1.16.0",
  "private": true,
  "description": "라스트티 — 취소된 골프 자리, 반값 참여 (Capacitor native wrapper)",
  "scripts": {
    "build:www": "node scripts/build-www.mjs",
    "sync": "npm run build:www && cap sync",
    "assets": "capacitor-assets generate --iconBackgroundColor #D6F94B --iconBackgroundColorDark #072418 --splashBackgroundColor #072418",
    "android": "npm run sync && cap open android",
    "apk:debug": "npm run sync && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
